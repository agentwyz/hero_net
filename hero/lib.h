#ifndef

#include <stdio.h>

#define SDS_MAX_PEMALLOC (1024 * 1024)


typedef char* sds

struct sdsdr {
    unsigned int len;
    unsigned int free;
    char buf[]; 
}

sds sdsnewLen(const void* inin)





#endif
